/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { voiceV1ConnectionPolicyTargetCreateConnectionPolicyTarget } from "../../funcs/voiceV1ConnectionPolicyTargetCreateConnectionPolicyTarget.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateConnectionPolicyTargetRequest$inboundSchema,
};

export const tool$voiceV1ConnectionPolicyTargetCreateConnectionPolicyTarget:
  ToolDefinition<typeof args> = {
    name: "voice-v1-connection-policy-target_create-connection-policy-target",
    description: ``,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await voiceV1ConnectionPolicyTargetCreateConnectionPolicyTarget(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value.result;

      return formatResult(value, apiCall);
    },
  };
